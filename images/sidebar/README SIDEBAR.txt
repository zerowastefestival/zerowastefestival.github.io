Folder: images/sidebar/

Content:

- put here the images that should appear on the left side (desktop version) or the bottom (mobile version) of posts on the website
- recommended size: 300 x 450 pixels (portrait)
- update "_include/advertising.html" with the link to the image to update all pages where it is used
- toggle the use the sidebar image in each post/article preamble with "ads: true" or "ads: false"